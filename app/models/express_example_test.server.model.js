'use strict';

/**
 * Module dependencies.
 */
var mongoose = require('mongoose'),
	Schema = mongoose.Schema;

/**
 * ExpressExampleTest Schema
 */
var ExpressExampleTestSchema = new Schema({

});

mongoose.model('ExpressExampleTest', ExpressExampleTestSchema);